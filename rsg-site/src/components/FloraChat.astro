<!-- The token identifies the public Dify embed; it is not a backend API key. -->
<script is:inline>
  window.difyChatbotConfig = {
    token: 'AYa1KmdKGLRKzFqU',
    baseUrl: 'https://flora.rsg.com.tw',
    inputs: {},
    containerProps: {
      role: 'button',
      tabIndex: 0,
      ariaLabel: '開啟或關閉 Flora 聊天',
      title: 'Flora 聊天',
      onKeyDown(event) {
        if (event.key === 'Enter' || event.key === ' ') {
          event.preventDefault();
          event.currentTarget.click();
        }
      },
    },
  };
</script>
<script is:inline src="https://flora.rsg.com.tw/embed.min.js" id="AYa1KmdKGLRKzFqU" defer></script>

<style is:global>
  :root {
    --dify-chatbot-bubble-button-right: 24px;
    --dify-chatbot-bubble-button-bottom: 84px;
    --dify-chatbot-bubble-button-width: 80px;
    --dify-chatbot-bubble-button-height: 80px;
    --dify-chatbot-bubble-button-border-radius: 50%;
  }
  #dify-chatbot-bubble-button { background-color: #fff !important; }
  #dify-chatbot-bubble-button::before {
    content: '';
    position: absolute;
    inset: -4px;
    border: 2px solid #70cfcf;
    border-radius: 50%;
    pointer-events: none;
    animation: flora-halo 4s ease-in-out infinite;
  }
  #dify-chatbot-bubble-button::after {
    content: 'Flora 小幫手';
    position: absolute;
    right: calc(100% + 14px);
    top: 50%;
    transform: translateY(-50%);
    padding: 7px 12px;
    border: 1px solid #c8e8e7;
    border-radius: 20px;
    background: #fff;
    color: #2b2f56;
    box-shadow: 0 2px 8px rgb(0 0 0 / 8%);
    font: 500 14px/1.4 'Noto Sans TC', sans-serif;
    white-space: nowrap;
    pointer-events: none;
  }
  /* Preserved WordPress Flora artwork; keep Dify's open/close icon switching. */
  #dify-chatbot-bubble-button #openIcon {
    width: 100%;
    height: 100%;
    background: url('/media/2025/07/FLORA-1-1.png') center / contain no-repeat;
    animation: flora-breathe 4s ease-in-out infinite;
  }
  #dify-chatbot-bubble-button #openIcon path { display: none; }
  #dify-chatbot-bubble-button #closeIcon { width: 32px; height: 32px; }
  #dify-chatbot-bubble-button #closeIcon path { stroke: #2b2f56; }
  #dify-chatbot-bubble-button:focus-visible { outline: 3px solid #37b8c8; outline-offset: 4px; }
  #dify-chatbot-bubble-button:is(:hover, :focus-visible)::before,
  #dify-chatbot-bubble-button:is(:hover, :focus-visible) #openIcon { animation-play-state: paused; }
  /* Dify switches the close icon's inline display when the chat is open. */
  #dify-chatbot-bubble-button:has(#closeIcon[style*='block'])::before,
  #dify-chatbot-bubble-button:has(#closeIcon[style*='block'])::after { display: none; }
  #dify-chatbot-bubble-button:has(#closeIcon[style*='block']) #openIcon { animation: none; }
  @keyframes flora-breathe {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.04); }
  }
  @keyframes flora-halo {
    0%, 100% { transform: scale(1); opacity: .25; }
    50% { transform: scale(1.09); opacity: .65; }
  }
  @media (prefers-reduced-motion: reduce) {
    #dify-chatbot-bubble-button::before { animation: none; opacity: .35; }
    #dify-chatbot-bubble-button #openIcon { animation: none; }
  }
  #dify-chatbot-bubble-window {
    position: fixed !important;
    top: auto !important;
    right: 24px !important;
    bottom: 176px !important;
    width: 24rem !important;
    height: 40rem !important;
    min-width: 0 !important;
    min-height: 0 !important;
    max-width: calc(100% - 48px) !important;
    max-height: calc(100vh - 192px) !important;
    max-height: calc(100dvh - 192px) !important;
  }
  @media (max-width: 640px) {
    :root {
      --dify-chatbot-bubble-button-right: 16px;
      --dify-chatbot-bubble-button-bottom: 80px;
      --dify-chatbot-bubble-button-width: 72px;
      --dify-chatbot-bubble-button-height: 72px;
    }
    #dify-chatbot-bubble-window {
      right: 16px !important;
      bottom: 164px !important;
      max-width: calc(100% - 32px) !important;
      max-height: calc(100vh - 180px) !important;
      max-height: calc(100dvh - 180px) !important;
    }
  }
</style>
